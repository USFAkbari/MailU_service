# Mailu Environment Configuration
# This file is used by Docker Compose to configure the Mailu services.

# Main configuration
# ------------------

# Set to a valid domain for production (e.g. example.com)
# For local testing, you can use 'localhost'
DOMAIN=localhost

# Hostnames for the server, separated by commas
# For local testing, use 'localhost'
HOSTNAMES=localhost

# Postmaster email address (e.g. admin@example.com)
POSTMASTER=admin@localhost

# Choose how to handle TLS certificates
# 'cert' for manual certificates (required for localhost)
# 'letsencrypt' for automatic certificates (requires public IP and valid domain)
TLS_FLAVOR=cert

# Authentication
# --------------

# Secret key for session management. 
# MUST be a long random string. Generate with: openssl rand -hex 16
SECRET_KEY=change_me_to_a_long_random_string

# Password scheme (PBKDF2, BLF-CRYPT, SHA512-CRYPT, SHA256-CRYPT)
PASSWORD_SCHEME=PBKDF2

# Initial Admin Account
# ---------------------
# These variables create the admin account on first run.

# Username part of the admin email (e.g. 'admin' for admin@localhost)
INITIAL_ADMIN_ACCOUNT=admin

# Domain part of the admin email (usually matches DOMAIN)
INITIAL_ADMIN_DOMAIN=localhost

# Password for the admin account
INITIAL_ADMIN_PW=password

# Creation mode: 'create' (always create), 'ifmissing' (only if not exists), 'update' (update existing)
INITIAL_ADMIN_MODE=ifmissing

# Mail settings
# -------------

# Message size limit in bytes (e.g. 50000000 for 50MB)
MESSAGE_SIZE_LIMIT=50000000

# Network settings
# ----------------

# Subnet for the docker network (must match docker-compose.yml)
SUBNET=192.168.203.0/24

# Webmail
# -------

# Webmail variant (roundcube, snappymail)
WEBMAIL=snappymail

# Antivirus
# ---------

# Enable antivirus (true/false)
ANTIVIRUS=true
